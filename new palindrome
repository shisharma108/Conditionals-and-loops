def checkPalindrome(num):
    rev=0;last=0
    while num!=0:
      last=num%10
      rev=rev*10+last
      num=num//10	
    return rev
num=int(input())
rev = checkPalindrome(num)
if(num==rev):
	 print('true')
else:
	 print('false')
